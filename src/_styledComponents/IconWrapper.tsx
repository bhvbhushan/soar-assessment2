import { styled } from '@mui/material';

export const IconWrapper = styled('div')(() => ({
  background: '#f5f7fb',
  borderRadius: '50%',
  padding: '1rem',
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
}));
