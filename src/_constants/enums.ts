export enum trxLogoTypeEnum {
  card = 'card',
  deposit = 'deposit',
  paypal = 'paypal',
}

export enum trxTypeEnum {
  debit = 'Debit',
  credit = 'Credit',
}

export enum fieldInputTypeEnum {
  text = 'text',
  password = 'passwrord',
  date = 'date',
}
